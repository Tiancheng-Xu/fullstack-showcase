diff --git a/package.json b/package.json
new file mode 100644
--- /dev/null
+++ b/package.json
@@
+{
+  "name": "course-homework",
+  "private": true,
+  "scripts": {
+    "dev": "pnpm --dir apps/web dev:web",
+    "test": "pnpm --dir apps/web/apps/web test",
+    "typecheck": "pnpm --dir apps/web/apps/web check-types",
+    "build": "pnpm --dir apps/web/apps/web build"
+  },
+  "packageManager": "pnpm@11.17.0"
+}
diff --git a/apps/web/bts.jsonc b/apps/web/bts.jsonc
new file mode 100644
--- /dev/null
+++ b/apps/web/bts.jsonc
@@
+{
+  "version": "3.38.0",
+  "frontend": ["tanstack-router"],
+  "backend": "none",
+  "runtime": "none",
+  "database": "none",
+  "orm": "none",
+  "auth": "none",
+  "payments": "none",
+  "packageManager": "pnpm",
+  "api": "none"
+}
diff --git a/apps/web/apps/web/package.json b/apps/web/apps/web/package.json
new file mode 100644
--- /dev/null
+++ b/apps/web/apps/web/package.json
@@
+{
+  "name": "web",
+  "private": true,
+  "scripts": {
+    "dev": "vite dev",
+    "build": "vite build",
+    "check-types": "vite build && tsc --noEmit",
+    "test": "vitest run"
+  },
+  "dependencies": {
+    "@tanstack/react-router": "^1.170.18",
+    "react": "^19.2.8",
+    "react-dom": "^19.2.8",
+    "tailwindcss": "^4.3.3"
+  },
+  "devDependencies": {
+    "@testing-library/jest-dom": "^7.0.0",
+    "@testing-library/react": "^16.3.2",
+    "jsdom": "^30.0.0",
+    "typescript": "^6.0.3",
+    "vite": "^8.1.5",
+    "vitest": "^4.1.10"
+  }
+}
diff --git a/apps/web/apps/web/vite.config.ts b/apps/web/apps/web/vite.config.ts
new file mode 100644
--- /dev/null
+++ b/apps/web/apps/web/vite.config.ts
@@
+import tailwindcss from "@tailwindcss/vite";
+import { tanstackRouter } from "@tanstack/router-plugin/vite";
+import react from "@vitejs/plugin-react";
+import { defineConfig } from "vitest/config";
+
+export default defineConfig({
+  server: { port: 3001 },
+  plugins: [tailwindcss(), tanstackRouter({ target: "react", autoCodeSplitting: true }), react()],
+  test: {
+    environment: "jsdom",
+    setupFiles: ["./src/test/setup.ts"],
+  },
+});
diff --git a/apps/web/apps/web/src/components/app-identity.tsx b/apps/web/apps/web/src/components/app-identity.tsx
new file mode 100644
--- /dev/null
+++ b/apps/web/apps/web/src/components/app-identity.tsx
@@
+export function AppIdentity() {
+  return <h1>育爱成长</h1>;
+}
diff --git a/apps/web/apps/web/src/components/__tests__/app-identity.test.tsx b/apps/web/apps/web/src/components/__tests__/app-identity.test.tsx
new file mode 100644
--- /dev/null
+++ b/apps/web/apps/web/src/components/__tests__/app-identity.test.tsx
@@
+import { render, screen } from "@testing-library/react";
+import { describe, expect, it } from "vitest";
+import { AppIdentity } from "../app-identity";
+
+describe("AppIdentity", () => {
+  it("keeps the product name visible to users", () => {
+    render(<AppIdentity />);
+    expect(screen.getByRole("heading", { name: "育爱成长" })).toBeVisible();
+  });
+});
