# CAP-004 / CAP-005 Final N6 Review

verdict: pass

## Remaining findings

None.

## Review basis

- The architecture viewer uses native `dialog.showModal()` for top-layer focus containment. Its fallback marks every other existing body subtree inert, preserves pre-existing inert state, removes only attributes it added, and restores focus to the initiating trigger.
- Entry focus, native cancel/Escape, backdrop dismissal, close-button dismissal, inert restoration, and trigger-focus restoration are covered by focused tests and the supplied in-app browser evidence.
- The iframe remains deferred until explicit activation and is restricted to `sandbox="allow-downloads allow-scripts"`; the supplied local Archify run reports successful interaction with no console error.
- Exactly eight project architecture IDs have matching JSON specifications, static PNG previews, and self-contained interactive HTML artifacts.
- Repository icons resolve through an explicit local manifest: 17 cached PNGs and four SVG-selected fallbacks. Same-key SVG alternates are documented as retained offline/low-color assets, and the local GitHub mark remains the terminal runtime fallback.
- Mobile dialog CSS applies viewport sizing and top/right/bottom/left safe-area handling.
- CAP-004 preserves the 21-repository open-source scene and the homepage's independent 48-skill scene. No reviewed change alters project status, evidence, release, or destination-link facts.

Provided evidence accepted: 13/13 focused tests, `tsc --noEmit`, diff check, and in-app browser interaction pass. These commands were not rerun during this read-only review.
