# Cosmos local chain

The local chain uses chain ID `babysteps-local-1`, one validator, and test-keyring roles
validator, Alice, and Bob. The reproducible Gate is `make -C apps/cosmos-local-chain test`.
Public evidence is exactly eight JSON files and contains no wallet recovery material.
