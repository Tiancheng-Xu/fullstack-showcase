# 2026-08-02 本地训练准备记录

## 目标

为“RAG 与 Agent 编排”作业准备 `Qwen/Qwen3-8B` QLoRA 训练基础。今天只在 Mac 完成数据与代码准备；模型训练留到用户在场时，使用 NVIDIA GPU 与 CUDA 远程环境执行。

## 今日完成

1. 在 `course-homework/homeworks/04-rag-agent-orchestration` 建立独立作业节点，保留原作业名称，并注明实际实现为 Qwen3、学习笔记知识库、Mastra 与 LangGraph。
2. 将“一灯学习笔记”设为严格只读来源。扫描 949 个 Markdown 文件，生成仅含路径、大小、时间和 SHA-256 的私有清单；扫描前后源仓库状态指纹一致。
3. 实现精确内容去重和出站敏感信息扫描。API Key、私钥、密码、手机号和邮箱只产生类别与位置，不在报告中回显匹配值。
4. 使用本机已有 Ollama 模型生成私有 SFT 草稿。生成不等于训练；所有草稿默认是 `draft`，只有显式审核批准后才能导出。
5. 实现来源分组切分和泄漏检查。同一来源或传递共享来源的样本不能跨越训练、验证和测试集合。
6. 实现版本化数据 release。增量训练必须记录基础模型、父数据 release、父 Adapter 哈希、新样本、回放样本和三份切分哈希。
7. 准备 NVIDIA CUDA 预检、首次 QLoRA、父 Adapter 增量续训、基础模型与 Adapter 成对评测及白名单产物收集脚本。
8. 实现严格上传白名单。完整学习笔记、Git 数据、凭据、原始审核元数据、基础模型权重和私有评测正文不能进入上传包或下载证据包。

## Agent 新增能力

该 Agent 能在不触碰只读知识源的前提下，准备可审核训练数据、构建可复现训练配置、执行隐私与泄漏检查，并在用户确认费用和设备后协助 NVIDIA CUDA QLoRA 训练。训练结束后，它能比较基础模型与 Adapter、验证产物哈希，并为下一次新增数据创建可追溯的增量 release。

## 尚未执行

- 未启动或操作 UU 远程机器。
- 未消耗 U 币。
- 未在 Mac 安装 CUDA 训练环境或运行 Qwen3 训练。
- 未产生真实 Adapter；真实 NVIDIA CUDA 训练与证据采集安排在下一次会话。
