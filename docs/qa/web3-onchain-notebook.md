# BabySteps 链上记事本验收记录

## 状态

| 验收项 | 状态 | 证据 |
|---|---|---|
| P0 本地链上便签实现 | ✅ | 合约、wagmi hook 与页面测试通过；280 UTF-8 字节限制、覆盖保存和清空当前值已实现 |
| P1 本地 BabySteps 实现 | ✅ | 三类活动、UTC+8 每日限制、成长星、四阶段星宝和独立活动界面已实现 |
| P1 本地可转积分实现 | ✅ | 双账本、直接赠送、余额读取、交易状态和赠送页面已通过专项测试 |
| 本地自动化验证 | ✅ | Web3 合约 25 项、前端 84 项；仓库结构 18 项、API 31 项、Web 20 项及 Go 全包测试通过 |
| 本地可视验收 | ⏳ | 待真实 Sepolia 合约地址和 Chrome + MetaMask 验收 |
| Sepolia 部署 | ⏳ | 未执行；无合约地址或部署交易可记录 |
| Etherscan 开源验证 | ⏳ | 未执行 |
| Sepolia P0 交互 | ⏳ | 待保存、刷新读取、覆盖和清空当前值 |
| Sepolia P1 交互 | ⏳ | 待记录活动、观察积分/阶段、重复提交错误和北京时间跨日规则 |
| Sepolia P1 积分转让交互 | ⏳ | 待两个专用测试地址完成赠送并核对双方余额与不变的成长阶段 |

这里的本地完成不代表 Sepolia 或 Etherscan 完成。只有直接观察到对应公开链上证据后，
才能更新外部状态。

## 已实现边界

- 活动枚举固定为 `Meal = 0`、`Walk = 1`、`Read = 2`，奖励为 3、5、7。
- 同一钱包的同一活动在 `UTC+8` 自然日内最多记录一次，钱包之间互不影响。
- 阶段阈值为 Egg 0–2、Sprout 3–7、Explorer 8–14、Star 15 及以上。
- 累计成长值只由本钱包活动增加且不因转出减少；可转余额可以在 Sepolia 地址间赠送。
- 收到的可转余额可以再次赠送，但不会增加收款钱包的累计成长值或星宝阶段。
- 成长星不是 ERC-20，没有授权、市场、价格、兑换、提现或主网部署声明。
- 活动调用没有儿童文本、照片、健康或位置字段；成长星没有价格、兑换、授权或提现能力。
- 便签和成长功能在合约、hook、页面与测试中保持独立。
- 清空便签只删除当前状态，历史交易和历史输入仍可能被公开追溯。

## 本地自动化证据

2026-08-03 在隔离工作树运行：

| 命令 | 结果 |
|---|---|
| `pnpm web3:check` | ✅ 合约 4 个检查文件、前端 34 个检查文件通过 |
| `pnpm web3:test` | ✅ 合约 25 项、前端 12 个文件共 84 项通过 |
| `pnpm web3:typecheck` | ✅ 合约与前端 TypeScript 检查通过 |
| `pnpm check` | ✅ 仓库 Biome 与 Go 格式检查通过 |
| `pnpm test` | ✅ 结构 18 项、API 31 项、Go 全包、Web 20 项通过 |
| `pnpm typecheck` | ✅ API、Go、Web 检查通过；Web 构建成功 |
| `pnpm build` | ✅ API、Go、现有 Web 生产构建成功 |
| `git diff --check` | ✅ 无空白错误 |

这里的 `pnpm build` 是现有课程主应用的仓库级构建，不等同于 BabySteps 生产构建。
`pnpm web3:build` 故意要求真实 `VITE_ONCHAIN_NOTEBOOK_ADDRESS`；部署前没有用占位
地址绕过这道门槛。

## 本地可视验收清单

- [ ] Chrome 打开本地页面，桌面与 320px 宽度无横向溢出。
- [ ] 只连接专用 MetaMask 测试账户，并显示 Sepolia 网络。
- [ ] P0：读取便签、保存、刷新恢复、覆盖、两步确认清空当前值。
- [ ] P0：页面明确说明历史交易仍公开，且拒绝超过 280 UTF-8 字节的内容。
- [ ] P1：读取真实成长星、阶段和当天三个活动状态。
- [ ] P1：钱包签名、广播等待、链上确认和错误状态均可见。
- [ ] P1：记录活动后积分按 3/5/7 增长，刷新后恢复。
- [ ] P1：同日重复活动显示安全错误，数据库式乐观伪增量不存在。
- [ ] P1：两个专用测试地址完成赠送，刷新后双方可转余额正确。
- [ ] P1：赠送前后双方累计成长值和星宝阶段均不因收发积分改变。
- [ ] P1：零地址、本人地址、零数量、小数和余额不足均被安全拒绝。
- [ ] 成功交易链接打开 Sepolia Etherscan 对应交易。
- [ ] 页面没有输入、显示或上传儿童隐私资料的入口。

## Sepolia 公开证据

部署后仅记录公开合约地址、部署交易、区块号、UTC 时间、Verified Contract URL 和
候选提交 SHA。不得记录私钥、助记词、RPC 凭据、API Key、儿童资料或真实资产钱包。
